"use client";

interface ServerIdPageProps {
  params: {
    serverId: string;
  };
}

const ServerIdPage = ({ params }: ServerIdPageProps) => {
  return (
    <div>
      <div>
        Server
      </div>
    </div>
  );
};

export default ServerIdPage;
